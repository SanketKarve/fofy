<nav class="nav has-shadow" id="nav">
    <div class="container">
        <div class="nav-left">
            <a class="nav-item" href="<%= '/forms/new' %>">
                <span class="icon">
                    <i class="fa fa-sticky-note"></i>
                </span>
                <h1 class="title">FoFy</h1>
            </a>
            <a class="nav-item <%= class_active('forms') %>" href="/forms">Forms</a>
            <a class="nav-item <%= class_active('charges') %>" href="/charges/new">Payment</a>
        </div>
        <div class="nav-right nav-menu">
            <div class="nav-item">
                <a class="button is-primary" href="/forms/new">
                    <span class="icon">
                        <i class="fa fa-plus"></i>
                    </span>
                    <span>New Form</span>
                </a>
                <span class="button">
                <% if user_signed_in? %>

                        <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>

                <% else %>

                        <%= link_to('Login', new_user_session_path)  %>

                <% end %>
                </span>
            </div>
        </div>
    </div>
</nav>
